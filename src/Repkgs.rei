module Pnpm: {
  [@genType]
  let streamPackageDirs: string => Wonka_types.sourceT(string);
};

module Rush: {
  [@genType]
  let streamPackageDirs: string => Wonka_types.sourceT(string);
};

module Yarn_V1: {
  [@genType]
  let streamPackageDirs: string => Wonka_types.sourceT(string);
};

module Yarn_V2: {
  [@genType]
  let streamPackageDirs: string => Wonka_types.sourceT(string);
};

[@genType]
let streamPackageDirs: string => Wonka_types.sourceT(string);